local EnumFlags = require("app.common.enumFlags")
local StringTable = require("app.common.stringTable")
local AssetPathTable = require("app.common.assetPathTable")
local Utils = require("app.common.utils")
local Constant = require("app.common.constant")

return  {
    enumFlags  = EnumFlags,
    stringTable  = StringTable,
    assetPathTable  = AssetPathTable,
    utils  = Utils,
    constant = Constant,
}